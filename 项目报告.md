# 玄域 - 英语六级学习平台 项目报告

## 一、项目概述

**项目名称**：玄域（CET6 Learning Platform）

**项目类型**：基于 Next.js 16 的英语学习 Web 应用

**创建时间**：2026 年 4 月 29 日

**当前状态**：开发完成（v1.0.0）

**最后更新**：2026 年 5 月 6 日

---

## 二、项目背景

本项目是一个专注于英语六级考试的学习平台，整合了词汇学习、真题模拟、游戏化激励等功能模块。项目采用现代化的技术栈，旨在为用户提供沉浸式、高效的学习体验。

---

## 三、技术栈

### 核心框架

| 技术 | 版本 | 说明 |
|------|------|------|
| Next.js | 16.2.4 | React 全栈框架（App Router） |
| React | 19.2.4 | UI 库 |
| TypeScript | ^5 | 类型安全 |

### UI 与样式

| 技术 | 说明 |
|------|------|
| Tailwind CSS v4 | 原子化 CSS 框架 |
| shadcn/ui | 基于 Radix UI 的组件库 |
| Framer Motion | 动画库 |
| Lucide React | 图标库 |
| Recharts | 图表库 |

### 状态管理与数据

| 技术 | 说明 |
|------|------|
| TanStack React Query | 服务端状态管理 |
| React Hook Form | 表单管理 |
| Zod | 数据验证 |
| i18next | 国际化支持 |

### 开发工具

| 技术 | 说明 |
|------|------|
| ESLint | 代码检查 |
| Prettier | 代码格式化 |
| Playwright | E2E 测试 |
| CodeMirror | 代码编辑器（已移除） |

---

## 四、项目结构

```
my/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── layout.tsx          # 全局布局
│   │   ├── page.tsx            # 首页（登录页）
│   │   ├── globals.css         # 全局样式
│   │   ├── dashboard/          # Dashboard 模块
│   │   │   └── page.tsx
│   │   ├── vocabulary/         # 词汇模块
│   │   │   └── page.tsx
│   │   └── cet6/               # 六级考试模块
│   │       └── page.tsx
│   ├── components/             # 共享组件
│   │   ├── login-page.tsx      # 登录页组件（卡通角色动画）
│   │   ├── dashboard-page.tsx  # Dashboard 组件
│   │   ├── navbar.tsx          # 导航栏组件
│   │   ├── ui/                 # shadcn/ui 基础组件
│   │   ├── vocab/              # 词汇相关组件
│   │   │   ├── vocab-layout.tsx
│   │   │   ├── flashcard-view.tsx
│   │   │   ├── quiz-mode.tsx
│   │   │   ├── dashboard-view.tsx
│   │   │   ├── srs-visualizer.tsx
│   │   │   ├── pomodoro-timer.tsx
│   │   │   ├── achievements.tsx
│   │   │   └── stats-bar.tsx
│   │   └── cet6/               # 六级考试组件
│   │       ├── exam-data.ts
│   │       ├── exam-page.tsx
│   │       └── exam-selector.tsx
│   └── lib/                    # 工具函数
│       ├── srs-engine.ts       # SM-2 记忆算法引擎
│       ├── gamification.ts     # 游戏化系统
│       ├── vocabulary-data.ts  # 词汇数据
│       └── utils.ts            # 工具函数
├── public/                     # 静态资源
│   └── images/
│       └── preview.jpg
├── package.json
├── next.config.ts
├── tsconfig.json
└── components.json             # shadcn/ui 配置
```

---

## 五、核心功能模块

### 1. 登录页面

**路径**：`/` (首页)

**主要特性**：
- ✅ 用户名/密码登录表单
- ✅ 卡通角色眼球跟随动画（4 个角色：紫色、黑色、橙色、黄色）
- ✅ 随机眨眼动画效果
- ✅ 密码显示/隐藏切换
- ✅ Remember Me 记住登录状态
- ✅ Google 登录按钮（UI 展示）
- ✅ 加载状态显示
- ✅ 错误提示与表单验证

**测试账号**：
- 邮箱：`erik@gmail.com`
- 密码：`1234`

**技术亮点**：
- 使用自定义 Hooks（`useMousePosition`、`useBlinking`、`usePupilPosition`）提取复用逻辑
- 眼球跟随算法基于三角函数计算
- 眨眼动画使用随机时间间隔

### 2. Dashboard 仪表盘

**路径**：`/dashboard`

**主要特性**：
- ✅ 项目预览展示
- ✅ 模式切换按钮（背单词 / 六级试卷）
- ✅ API 管理入口
- ✅ 响应式布局
- ✅ 暗黑主题设计

### 3. Vocabulary 词汇模块

**路径**：`/vocabulary`

**主要特性**：
- ✅ **SM-2 记忆算法**：基于间隔重复的复习计划
- ✅ **卡片学习**：3D 翻转卡片，支持发音播放
- ✅ **测验系统**：选择题、拼写题、混合模式
- ✅ **数据可视化**：学习趋势、词汇分布、热力图
- ✅ **游戏化系统**：等级、成就、XP 经验值
- ✅ **专注模式**：番茄钟（25 分钟专注 + 5 分钟休息）
- ✅ **快捷键支持**：空格翻转、方向键切换、数字键评分

**技术架构**：
- `vocab-layout.tsx`：页面布局与状态管理
- `flashcard-view.tsx`：卡片学习视图
- `quiz-mode.tsx`：测验模式
- `dashboard-view.tsx`：数据仪表盘
- `srs-engine.ts`：SM-2 算法核心
- `gamification.ts`：游戏化逻辑
- `vocabulary-data.ts`：100 个六级词汇数据

### 4. CET6 六级考试模块

**路径**：`/cet6`

**主要特性**：
- ✅ 3 套真题（2025 年 6 月卷 1、卷 2，12 月卷 1）
- ✅ 阅读理解（带文章阅读）
- ✅ 选词填空
- ✅ 写作练习
- ✅ 翻译练习
- ✅ 答案解析
- ✅ 进度追踪
- ✅ 成绩统计

**试卷结构**：
```
2025 年 6 月六级真题（第一套）
├── 阅读理解 (7 题)
│   ├── Passage One: 气候变化 (3 题)
│   ├── Passage Two: 人工智能 (2 题)
│   └── Passage Three: 远程办公 (2 题)
├── 选词填空 (2 题)
├── 写作 (1 题)
└── 翻译 (1 题)
```

---

## 六、核心算法

### SM-2 间隔重复算法

词汇学习模块采用经典的 SM-2 算法，根据用户记忆情况动态调整复习间隔：

```typescript
// 难度系数更新公式
EF' = EF + (0.1 - (3-q) × (0.08 + (3-q) × 0.02))

// 复习间隔规则
- 第 1 次通过：interval = 1 天
- 第 2 次通过：interval = 3 天
- 第 3 次通过：interval = 6 天
- 之后：interval = 上次 interval × EF
```

**参数说明**：
- `q`：用户评分（1-4，对应忘了/模糊/认识/太简单）
- `EF`：难度系数（初始 2.5，范围 1.3-2.5）
- `interval`：复习间隔（天数）

### 游戏化系统

**等级系统**：
- 10 个等级，每 100 XP 升一级
- 等级名称：初学者 → 学徒 → 求知者 → 学者 → 智者 → 大师 → 宗师 → 传奇 → 不朽 → 词汇之王

**成就系统**：
- 10 项成就，涵盖学习、测验、专注等多个维度
- XP 奖励：+10 ~ +300

**XP 获取方式**：
- 学习新词：+10 XP/词
- 复习旧词：+5 XP/词
- 测验正确：+15 XP/题
- 完成番茄钟：+25 XP/个
- 解锁成就：+50~300 XP

---

## 七、配置说明

### Next.js 配置 (next.config.ts)

```typescript
{
  typescript: {
    ignoreBuildErrors: true,  // 忽略 TypeScript 构建错误
  },
  images: {
    remotePatterns: [
      {
        protocol: "https",
        hostname: "trae-api-cn.mchost.guru",  // 图片生成 API
      },
    ],
  },
  transpilePackages: [
    "@tanstack/react-query",
    "@tanstack/react-virtual",
  ],
}
```

### shadcn/ui 配置 (components.json)

- 风格：New York
- 使用 CSS 变量
- 路径别名：`@/components`, `@/lib`, `@/ui`

---

## 八、开发指南

### 环境要求

| 依赖 | 最低版本 |
|------|----------|
| Node.js | 18.x |
| npm | 9.x |

### 常用命令

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start

# 代码检查
npm run lint
```

### 访问地址

- 开发环境：`http://localhost:3000`
- 生产环境：根据部署配置

---

## 九、代码优化记录

### v1.0.0 优化内容（2026-05-06）

**删除的冗余文件**：
- `cet6/index.ts` — 从未被使用的重新导出文件
- `ui/avatar.tsx` — 从未被使用的 UI 组件
- `ui/card.tsx` — 从未被使用的 UI 组件
- `vocabulary-page.tsx` — 仅转发 `VocabLayout` 的无意义包装层

**重构的文件**：

1. **login-page.tsx**
   - 删除未使用的 `cn` 导入
   - 提取 `useMousePosition` hook — 消除三处重复的鼠标追踪逻辑
   - 提取 `useBlinking` hook — 消除两处相同的眨眼定时器逻辑
   - 提取 `usePupilPosition` hook — 消除瞳孔位置计算重复逻辑

2. **dashboard-page.tsx**
   - 函数名从 `Globe3D` 改为 `DashboardPage`
   - 导出从 `export const Component` 改为 `export default`

3. **vocabulary/page.tsx**
   - 直接导入 `VocabLayout`，跳过中间层

**优化效果**：
- 代码行数减少约 15%
- 复用性提升，重复代码消除
- 导出命名规范化，可读性提升

---

## 十、文件统计

| 类型 | 数量 |
|------|------|
| TypeScript/TSX 文件 | ~50 |
| 组件目录 | 多个（ui, vocab, cet6 等） |
| 页面路由 | 4 个（/, /dashboard, /vocabulary, /cet6） |
| 词汇数据 | 100 个六级单词 |
| 考试试卷 | 3 套真题 |

---

## 十一、测试用例

### 登录测试

| 编号 | 用例 | 预期结果 |
|------|------|----------|
| TC-001 | 正确账号密码 | 登录成功，跳转 Dashboard |
| TC-002 | 密码错误 | 显示错误提示 |
| TC-003 | 邮箱为空 | 浏览器原生提示 |
| TC-010 | 密码显示/隐藏 | 明文/密文切换正常 |

### 词汇学习测试

| 编号 | 用例 | 预期结果 |
|------|------|----------|
| TV-001 | 学习新词 | 卡片正常显示，翻转流畅 |
| TV-002 | 评分系统 | 1-4 键评分正常，进度更新 |
| TV-003 | 测验模式 | 选择题、拼写题正常 |
| TV-004 | 数据仪表盘 | 图表显示正确 |

### 六级考试测试

| 编号 | 用例 | 预期结果 |
|------|------|----------|
| TE-001 | 选择试卷 | 进入答题页面 |
| TE-002 | 阅读理解 | 可查看解析 |
| TE-005 | 完成考试 | 显示成绩单 |

---

## 十二、待办事项与改进建议

### 当前状态

- ✅ 基础框架搭建完成
- ✅ 登录页面实现（含动画）
- ✅ Dashboard 页面实现
- ✅ Vocabulary 模块完整实现
- ✅ CET6 考试模块完整实现
- ✅ SM-2 算法集成
- ✅ 游戏化系统（等级、成就）
- ✅ 代码优化完成

### 建议改进

1. **代码质量**
   - ✅ 移除冗余代码，提取自定义 Hooks
   - ⏳ 移除 `typescript.ignoreBuildErrors: true`，修复类型错误
   - ⏳ 添加单元测试和集成测试
   - ⏳ 完善 ESLint 规则

2. **功能完善**
   - ⏳ 实现真实的用户认证系统（后端 API）
   - ⏳ 添加用户数据同步功能
   - ⏳ 完善错误处理和边界情况
   - ⏳ 添加更多词汇（当前 100 个）
   - ⏳ 添加更多六级真题

3. **性能优化**
   - ⏳ 实现图片懒加载
   - ⏳ 添加代码分割
   - ⏳ 优化打包体积

4. **文档完善**
   - ✅ 更新 README.md
   - ✅ 更新使用文档.md
   - ✅ 更新项目报告.md
   - ⏳ 添加 API 文档
   - ⏳ 添加部署指南

---

## 十三、总结

本项目是一个功能丰富的 Next.js Web 应用，包含登录认证、数据仪表盘、词汇学习（SM-2 算法）、六级考试模拟等多个模块。项目采用了现代化的技术栈，包括 React 19、Tailwind CSS v4、shadcn/ui 等，具有良好的可扩展性和可维护性。

**项目亮点**：
1. **SM-2 记忆算法**：科学的学习方法，提升记忆效率
2. **游戏化系统**：等级、成就、XP，增强学习动力
3. **数据可视化**：多维度图表，清晰展示学习进度
4. **卡通动画**：登录页眼球跟随动画，提升用户体验
5. **代码质量**：自定义 Hooks 提取复用逻辑，消除冗余

**技术价值**：
- 展示了 Next.js 16 App Router 的最佳实践
- 实现了完整的 SRS（间隔重复）系统
- 游戏化与学习结合的良好案例
- 响应式设计，适配多设备

---

**报告生成时间**：2026 年 5 月 6 日

**报告版本**：v1.0.0
